module.exports = {
  printWidth: 100, // Limits line length to 100 characters.
  tabWidth: 2, // Set indentation size to 2 spaces.
  useTabs: false, // Use spaces instead of tabs.
  semi: true, // Add semicolons at the end of statements.
  singleQuote: true, // Use single quotes instead of double quotes.
  trailingComma: 'all', // Add trailing commas wherever possible.
  bracketSpacing: true, // Include spaces between brackets in object literals.
  arrowParens: 'avoid', // Avoid parentheses when a single argument is present in arrow functions.
  proseWrap: 'preserve', // Do not modify markdown text wrapping.
  endOfLine: 'lf', // Use line feed (\n) as the end-of-line character.
};

